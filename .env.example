# WordPress Publishing Service - Environment Variables
# 复制此文件为 .env 并填入实际值
# 注意: .env 文件已在 .gitignore 中，不会被提交到 Git

# ==================== WordPress 配置 ====================

# WordPress 站点 URL (不要以 / 结尾)
WORDPRESS_URL=http://localhost:8000

# WordPress 管理员用户名
WORDPRESS_USERNAME=admin

# WordPress 管理员密码
WORDPRESS_PASSWORD=admin_password_123

# ==================== Anthropic API 配置 ====================

# Anthropic API Key (用于 Computer Use)
# 获取方式: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Computer Use 模型
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# API 超时时间 (秒)
ANTHROPIC_TIMEOUT=300

# ==================== 应用配置 ====================

# 运行环境 (development, staging, production)
ENVIRONMENT=development

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=logs/app.log

# 是否启用详细日志
DEBUG_MODE=true

# ==================== Provider 配置 ====================

# 默认 Provider (playwright, computer_use)
DEFAULT_PROVIDER=playwright

# 是否启用降级机制
ENABLE_FALLBACK=true

# Playwright 重试次数
PLAYWRIGHT_MAX_RETRIES=3

# Playwright 超时时间 (毫秒)
PLAYWRIGHT_TIMEOUT=30000

# 是否使用无头模式
PLAYWRIGHT_HEADLESS=false

# Computer Use 重试次数
COMPUTER_USE_MAX_RETRIES=2

# ==================== 浏览器配置 ====================

# 浏览器类型 (chromium, firefox, webkit)
BROWSER_TYPE=chromium

# 浏览器窗口宽度
BROWSER_WIDTH=1920

# 浏览器窗口高度
BROWSER_HEIGHT=1080

# 是否启用浏览器 DevTools
BROWSER_DEVTOOLS=false

# 浏览器下载路径
BROWSER_DOWNLOAD_PATH=./downloads

# ==================== 截图和审计配置 ====================

# 是否启用截图
ENABLE_SCREENSHOTS=true

# 截图保存路径
SCREENSHOT_PATH=./screenshots

# 截图质量 (0-100)
SCREENSHOT_QUALITY=90

# 是否启用审计追踪
ENABLE_AUDIT_TRAIL=true

# 审计日志路径
AUDIT_LOG_PATH=logs/audit.json

# ==================== 文件上传配置 ====================

# 上传文件临时目录
UPLOAD_TEMP_DIR=./uploads/temp

# 上传文件最大大小 (MB)
UPLOAD_MAX_SIZE_MB=10

# 允许的图片格式 (逗号分隔)
ALLOWED_IMAGE_FORMATS=jpg,jpeg,png,gif,webp,bmp

# ==================== FastAPI 服务器配置 ====================

# API 服务器主机
API_HOST=0.0.0.0

# API 服务器端口
API_PORT=8080

# API Workers 数量
API_WORKERS=4

# 是否启用 API 文档
API_DOCS_ENABLED=true

# API 文档路径
API_DOCS_URL=/docs

# CORS 允许的源 (逗号分隔，* 表示全部)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ==================== 数据库配置 (如需要) ====================

# 数据库 URL (SQLite 示例)
# DATABASE_URL=sqlite:///./publishing.db

# PostgreSQL 示例
# DATABASE_URL=postgresql://user:password@localhost/dbname

# ==================== Redis 配置 (如需要任务队列) ====================

# Redis 主机
# REDIS_HOST=localhost

# Redis 端口
# REDIS_PORT=6379

# Redis 密码
# REDIS_PASSWORD=

# Redis 数据库编号
# REDIS_DB=0

# ==================== Prometheus 监控配置 ====================

# 是否启用 Prometheus 指标
ENABLE_METRICS=true

# Prometheus 指标端口
METRICS_PORT=9090

# Prometheus 指标路径
METRICS_PATH=/metrics

# ==================== 性能和限制配置 ====================

# 最大并发任务数
MAX_CONCURRENT_TASKS=3

# 任务超时时间 (秒)
TASK_TIMEOUT=600

# 自动保存间隔 (秒)
AUTOSAVE_INTERVAL=30

# 页面加载超时 (秒)
PAGE_LOAD_TIMEOUT=60

# 元素等待超时 (秒)
ELEMENT_WAIT_TIMEOUT=10

# ==================== 测试环境配置 ====================

# 测试 WordPress URL
TEST_WORDPRESS_URL=http://localhost:8001

# 测试用户名
TEST_WORDPRESS_USERNAME=admin

# 测试密码
TEST_WORDPRESS_PASSWORD=password

# 是否在测试后清理数据
TEST_CLEANUP=true

# ==================== 选择器配置路径 ====================

# Selectors YAML 文件路径
SELECTORS_CONFIG_PATH=config/selectors.yaml

# Instructions YAML 文件路径
INSTRUCTIONS_CONFIG_PATH=config/instructions.yaml

# ==================== Webhook 配置 (可选) ====================

# 发布成功 Webhook URL
# SUCCESS_WEBHOOK_URL=https://example.com/webhooks/success

# 发布失败 Webhook URL
# ERROR_WEBHOOK_URL=https://example.com/webhooks/error

# Webhook 超时时间 (秒)
# WEBHOOK_TIMEOUT=10

# ==================== Sentry 错误追踪 (可选) ====================

# Sentry DSN
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# Sentry 环境
# SENTRY_ENVIRONMENT=development

# Sentry 采样率 (0.0 - 1.0)
# SENTRY_TRACES_SAMPLE_RATE=0.1

# ==================== 功能开关 ====================

# 是否启用 Computer Use Provider
ENABLE_COMPUTER_USE=true

# 是否启用 Playwright Provider
ENABLE_PLAYWRIGHT=true

# 是否启用图片优化
ENABLE_IMAGE_OPTIMIZATION=false

# 是否启用自动 SEO 建议
ENABLE_AUTO_SEO_SUGGESTIONS=false

# 是否启用内容校对
ENABLE_CONTENT_PROOFREADING=false
